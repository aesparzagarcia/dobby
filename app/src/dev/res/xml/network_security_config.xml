<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!-- Dev flavor: allow HTTP to local backend (emulator, LAN IP, adb reverse). -->
    <base-config cleartextTrafficPermitted="true" />
</network-security-config>
