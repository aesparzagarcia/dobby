package com.ares.ewe.core.pricing

/**
 * Valores por defecto locales si la API de configuración no está disponible.
 */
object DeliveryPricingConfig {
    val DEFAULT: DeliveryPricingSettings get() = DeliveryPricingSettings.DEFAULT
}
